import React from "react";

const AdministrationStarter = ({ title }) => {
  return (
    <div className="bg-secondary text-center text-white font-bold text-lg v-sm:text-2xl py-4 mb-6">
      {title}
    </div>
  );
};

export default AdministrationStarter;
